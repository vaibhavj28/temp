spring:  
  application:
    name: web client bff
  output:
    ansi:
      enabled: always
  sleuth:
    sampler:
      probability: 1
  zipkin:
    base-url: http://localhost:9411/      
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
